# TaskManager v1.2 日志审计设计文档

## 功能概述

实现日志审计功能，记录所有关键操作，提供审计日志查询。

## 技术方案

### 1. 数据库设计

#### 1.1 AuditLog 表

```prisma
model AuditLog {
  id          String   @id @default(uuid())
  userId      String?
  action      String
  resource    String
  resourceId  String?
  details     String?
  ipAddress   String?
  userAgent   String?
  status      String   @default("success")
  createdAt   DateTime @default(now())

  user        User?    @relation(fields: [userId], references: [id])

  @@map("audit_logs")
}
```

### 2. 审计日志类型

#### 2.1 用户操作

- 用户注册
- 用户登录
- 用户登出
- 用户更新

#### 2.2 任务操作

- 任务创建
- 任务更新
- 任务删除
- 任务完成

#### 2.3 系统操作

- 系统启动
- 系统关闭
- 配置更改

### 3. 审计日志记录

```typescript
// 记录审计日志
export const logAudit = async (data: {
  userId?: string;
  action: string;
  resource: string;
  resourceId?: string;
  details?: string;
  ipAddress?: string;
  userAgent?: string;
  status?: string;
}) => {
  await prisma.auditLog.create({
    data
  });
};
```

### 4. 审计日志查询

```typescript
// 查询审计日志
export const getAuditLogs = async (filters: {
  userId?: string;
  action?: string;
  resource?: string;
  startDate?: Date;
  endDate?: Date;
  page?: number;
  pageSize?: number;
}) => {
  const where: any = {};

  if (filters.userId) {
    where.userId = filters.userId;
  }

  if (filters.action) {
    where.action = filters.action;
  }

  if (filters.resource) {
    where.resource = filters.resource;
  }

  if (filters.startDate || filters.endDate) {
    where.createdAt = {};
    if (filters.startDate) {
      where.createdAt.gte = filters.startDate;
    }
    if (filters.endDate) {
      where.createdAt.lte = filters.endDate;
    }
  }

  const logs = await prisma.auditLog.findMany({
    where,
    orderBy: { createdAt: 'desc' },
    skip: ((filters.page || 1) - 1) * (filters.pageSize || 20),
    take: filters.pageSize || 20
  });

  const total = await prisma.auditLog.count({ where });

  return { logs, total };
};
```

## 实现步骤

### 1. 更新 Prisma Schema

添加 AuditLog 表。

### 2. 创建审计日志服务

创建审计日志服务，提供日志记录和查询功能。

### 3. 集成到现有功能

在关键操作中添加审计日志记录。

### 4. 创建审计日志查询 API

提供审计日志查询接口。

### 5. 创建审计日志管理页面

提供审计日志管理界面。

## 安全考虑

1. **日志完整性**：确保日志不被篡改
2. **日志保密性**：确保敏感信息不被泄露
3. **日志可用性**：确保日志随时可查
4. **日志保留**：设置合理的日志保留期限

## 测试

### 1. 功能测试

- ✅ 审计日志记录正常
- ✅ 审计日志查询正常
- ✅ 审计日志过滤正常

### 2. 安全测试

- ✅ 日志不被篡改
- ✅ 敏感信息不泄露
- ✅ 日志随时可查

---

**文档版本**: 1.0
**创建日期**: 2026-03-31
**创建人**: 白晶晶

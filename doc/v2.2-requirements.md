# TaskManager v2.2 需求文档

> **版本**: 2.2.0
> **创建日期**: 2026-04-02
> **状态**: 开发中

---

## 1. 需求概述

TaskManager v2.2 版本旨在增加以下9个核心功能，提升任务管理的效率和用户体验。

---

## 2. 核心需求

### 2.1 任务时间线 (Timeline)

**需求描述**:
- 为每个任务创建完整的时间线，记录所有操作历史
- 时间线节点包括：创建、开始、评论、编辑、状态变更、完成、分配、收藏
- 时间线显示在任务详情页
- 支持按时间顺序展示操作记录

**用户故事**:
作为用户，我希望能够看到任务的所有操作历史，以便了解任务的进展和变化。

**技术实现**:
- 后端：创建 Timeline 表，记录所有任务操作
- 前端：TaskTimeline 组件，按时间展示操作记录
- API：获取任务时间线，创建时间线节点，删除时间线节点

---

### 2.2 手势交互 (Gesture Interaction)

**需求描述**:
- 在任务卡片上支持手势操作
- 向右滑动：切换任务状态（未开始 → 进行中 → 已完成 → 未开始）
- 向左滑动：显示任务时间线
- 支持移动端触摸和PC端鼠标拖拽

**用户故事**:
作为用户，我希望能够通过滑动快速切换任务状态或查看时间线，提高操作效率。

**技术实现**:
- 前端：SwipeableTaskCard 组件，集成手势处理
- Hook：useSwipeGesture，处理触摸和鼠标事件
- 动画：Framer Motion，流畅的滑动动画效果

---

### 2.3 情境（项目）管理 (Context Management)

**需求描述**:
- 支持创建、编辑、删除情境（项目）
- 每个任务属于一个情境
- 支持在情境之间切换
- 显示当前情境的所有任务

**用户故事**:
作为用户，我希望能够创建不同的项目（情境），并在项目之间切换，以便更好地组织任务。

**技术实现**:
- 后端：Context 表，Context CRUD API
- 前端：ContextSwitcher 组件，显示和切换情境
- 状态管理：Zustand Store，保存当前情境

---

### 2.4 情境切换组件 (Context Switcher)

**需求描述**:
- 显示所有可用的情境列表
- 支持快速切换当前情境
- 高亮显示当前激活的情境
- 显示情境的任务数量

**用户故事**:
作为用户，我希望能够快速切换当前项目，查看不同项目的任务。

**技术实现**:
- 前端：ContextSwitcher 组件
- API：获取用户的所有情境
- 状态管理：更新当前情境，重新加载任务列表

---

### 2.5 用户头像 (User Avatar)

**需求描述**:
- 显示用户的头像、昵称
- 支持第三方登录的昵称显示
- 点击头像进入个人中心
- 支持上传头像（v2.3）

**用户故事**:
作为用户，我希望能够看到我的头像和昵称，并且点击后可以进入个人中心查看我的信息。

**技术实现**:
- 后端：User 表的 avatar、nickname 字段
- 前端：UserAvatar 组件，显示头像和昵称
- API：获取用户信息，更新用户信息

---

### 2.6 任务预计耗时 (Estimated Time)

**需求描述**:
- 为任务设置预计完成时间
- 支持时间单位：小时(h)、天(d)、周(w)、月(mo)、季度(q)、年(y)
- 显示为格式化字符串（如"8小时"、"3天"）
- 在任务卡片和详情页显示

**用户故事**:
作为用户，我希望能够为每个任务设置预计完成时间，以便更好地规划工作。

**技术实现**:
- 后端：Task 表的 estimated_time 字段（value + unit）
- 前端：EstimatedTimeSelector 组件，选择时间和单位
- 格式化：timeFormatter 工具，转换为显示字符串

---

### 2.7 任务角色信息 (Task Roles)

**需求描述**:
- 每个任务支持三种角色：执行者(assignee)、报告人(reporter)、验证人(verifier)
- 在任务详情页显示角色信息
- 支持选择不同的用户担任不同角色
- 显示角色的头像和昵称

**用户故事**:
作为用户，我希望能够为任务分配执行者、报告人和验证人，以便明确责任分工。

**技术实现**:
- 后端：Task 表的 assignee_id、reporter_id、verifier_id 字段
- 前端：RoleSelector 组件，选择用户角色
- API：获取用户列表，更新任务角色

---

### 2.8 任务收藏功能 (Task Favorite)

**需求描述**:
- 支持收藏/取消收藏任务
- 在任务卡片上显示收藏按钮（五角星图标）
- 收藏状态用黄色五角星表示，未收藏用灰色五角星
- 点击收藏按钮切换收藏状态，带动画效果
- 支持筛选收藏的任务

**用户故事**:
作为用户，我希望能够收藏重要的任务，以便快速访问和查看。

**技术实现**:
- 后端：Task 表的 is_favorite 字段
- 前端：收藏按钮在任务卡片上
- API：更新任务收藏状态
- 筛选：支持只查看收藏的任务

---

### 2.9 用户个人中心 (User Profile)

**需求描述**:
- 个人中心页面，显示用户信息
- 支持编辑昵称、个人描述
- 显示用户所属的组织架构
- 显示用户的角色信息
- 显示用户的统计数据（任务数、完成任务数等）
- 支持标签页切换（个人信息、组织架构、角色信息、统计数据）

**用户故事**:
作为用户，我希望能够在个人中心查看和编辑我的信息，包括我的组织、角色和任务统计。

**技术实现**:
- 后端：User 表的 nickname、description 字段；关联组织、角色
- 前端：UserProfilePage 组件，多标签页设计
- API：获取用户信息、更新用户信息、获取统计数据

---

## 3. 技术需求

### 3.1 后端技术栈
- Node.js v18+
- Express.js
- TypeScript
- PostgreSQL
- Prisma ORM

### 3.2 前端技术栈
- React 18
- TypeScript
- Vite
- Zustand（状态管理）
- TanStack Query（数据获取）
- Ant Design Mobile（UI组件）
- Framer Motion（动画）
- Lucide React（图标）

### 3.3 数据库设计

#### 新增表
- **timeline**: 记录任务操作历史
  - id: UUID
  - task_id: UUID (外键到tasks表)
  - action: 枚举 (create/start/comment/edit/status_change/complete/assign/favorite)
  - actor_id: UUID (操作者ID)
  - details: JSON (操作详情)
  - created_at: TIMESTAMP

- **contexts**: 情境（项目）表
  - id: UUID
  - name: VARCHAR(100)
  - description: TEXT
  - owner_id: UUID (外键到users表)
  - avatar: TEXT
  - is_public: BOOLEAN
  - created_at: TIMESTAMP
  - updated_at: TIMESTAMP

#### 修改表
- **tasks**: 任务表
  - 添加 context_id: UUID (外键到contexts表)
  - 添加 assignee_id: UUID (外键到users表)
  - 添加 reporter_id: UUID (外键到users表)
  - 添加 verifier_id: UUID (外键到users表)
  - 添加 estimated_time: JSON (value + unit)
  - 添加 is_favorite: BOOLEAN

- **users**: 用户表
  - 添加 nickname: VARCHAR(50)
  - 添加 description: TEXT
  - 添加 avatar: TEXT

---

## 4. API设计

### 4.1 时间线API
- `GET /api/v1/timelines/task/:taskId` - 获取任务时间线
- `POST /api/v1/timelines` - 创建时间线节点
- `POST /api/v1/timelines/batch` - 批量创建时间线节点
- `DELETE /api/v1/timelines/:id` - 删除时间线节点
- `GET /api/v1/timelines/user/:userId` - 获取用户时间线

### 4.2 情境API
- `GET /api/v1/contexts` - 获取用户的所有情境
- `POST /api/v1/contexts` - 创建情境
- `PUT /api/v1/contexts/:id` - 更新情境
- `DELETE /api/v1/contexts/:id` - 删除情境

### 4.3 用户API
- `GET /api/v1/users/profile` - 获取当前用户信息
- `PUT /api/v1/users/profile` - 更新用户信息
- `GET /api/v1/users/organizations` - 获取用户所属组织
- `GET /api/v1/users/roles` - 获取用户的角色
- `GET /api/v1/users/stats` - 获取用户统计数据

### 4.4 任务API
- `GET /api/v1/tasks` - 获取任务列表（支持按情境、收藏筛选）
- `POST /api/v1/tasks` - 创建任务
- `PUT /api/v1/tasks/:id` - 更新任务（支持更新角色、预计耗时、收藏状态）

---

## 5. UI/UX设计

### 5.1 任务列表页
- 顶部：情境切换器
- 中间：任务列表（支持滑动交互）
- 底部：添加任务按钮

### 5.2 任务详情页
- 标题：任务标题
- 信息：预计耗时、角色信息
- 操作：编辑、删除、收藏
- 时间线：任务操作历史

### 5.3 个人中心页
- 标签页：个人信息、组织架构、角色信息、统计数据
- 个人信息：头像、昵称、描述（可编辑）
- 统计数据：总任务数、完成任务数、创建的任务数、收藏的任务数

---

## 6. 测试要求

### 6.1 单元测试
- 后端：服务层测试、控制器测试、中间件测试
- 前端：组件测试、Hook测试、工具函数测试
- 目标覆盖率：后端≥80%，前端≥70%

### 6.2 集成测试
- API端点测试
- 数据库操作测试
- 前后端交互测试

### 6.3 E2E测试
- 用户登录流程
- 任务创建、编辑、删除流程
- 情境切换流程
- 手势交互流程
- 目标通过率：≥90%

---

## 7. 性能要求

### 7.1 前端性能
- 首屏加载时间 < 3秒
- 列表滑动帧率 ≥ 60fps
- API响应时间 < 500ms
- Lighthouse性能评分 ≥ 90

### 7.2 后端性能
- API响应时间 < 200ms
- 数据库查询时间 < 100ms
- 支持并发用户数 ≥ 100

---

## 8. 安全要求

### 8.1 认证和授权
- JWT认证
- 基于角色的访问控制（RBAC）
- 保护敏感API端点

### 8.2 数据安全
- 输入验证
- SQL注入防护
- XSS防护
- CSRF防护

---

## 9. 开发计划

### Week 1: 数据层和API层
- Day 1: 数据库设计和迁移
- Day 2: 后端基础搭建
- Day 3: 服务层开发
- Day 4: 控制器和路由
- Day 5: API测试和文档

### Week 2: 核心功能开发
- Day 6-7: 任务时间线系统
- Day 8-9: 手势交互
- Day 10: 情境管理

### Week 3: 高级功能和UI优化
- Day 11: 用户头像组件
- Day 12: 预计耗时功能
- Day 13: 任务角色信息
- Day 14: 任务收藏功能
- Day 15: UI优化和整合

### Week 4: 测试和部署
- Day 16-17: 个人中心开发
- Day 18: 全面测试
- Day 19: Bug修复和优化
- Day 20: 部署和上线

---

**文档维护者**: 白晶晶
**最后更新**: 2026-04-02
**版本**: 2.2.0
